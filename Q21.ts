// Create an object to store a person's information
const person: { 
    name: string, 
    age: number, 
    address: string 
  } = { 
    name: 'Ehsaan', 
    age: 20, 
    address: '123 Main St' 
  };
  
  // Create an object to store a shopping list
  const shoppingList: {
    item1: string,
    item2: string,
    item3: string
  } = {
    item1: 'milk',
    item2: 'bread',
    item3: 'eggs'
  };
  
  // Create an object to store a student's grades
  const grades: {
    math: number,
    science: number,
    history: number
  } = {
    math: 90,
    science: 85,
    history: 95
  };
  
  console.log(person);
  console.log(shoppingList);
  console.log(grades);
  